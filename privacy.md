# 隐私说明

ChatGPT 长对话助手是一个本地运行的浏览器扩展。它的设计目标是整理 ChatGPT 长对话的页面阅读状态，而不是分析、收集或同步聊天内容。

## 运行范围

扩展只在以下页面运行：

- `https://chatgpt.com/*`
- `https://chat.openai.com/*`

扩展不会读取其他网站，也不请求 `<all_urls>` 权限。

## 数据处理方式

扩展只处理当前页面上的 DOM 节点，用于判断哪些对话 turn 应该完整显示，哪些较早 turn 可以收起成占位块，并生成本地目录、标记状态和导出预览。

扩展不会：

- 上传聊天内容。
- 保存聊天内容。
- 分析聊天内容语义。
- 拦截 ChatGPT 后端接口。
- 改写 ChatGPT 返回的 conversation JSON。
- 使用服务器。
- 使用 analytics、tracking 或统计 SDK。
- 加载远程脚本或外部 CDN。

所有优化逻辑都在浏览器本地执行。

## 存储内容

扩展会保存少量功能设置，例如：

- 是否启用助手。
- 最近保留多少条 turn。
- 是否启用向上滚动自动显示旧消息。
- 是否启用长用户消息自动收起。
- 设置卡片是否展开。
- 阅读过滤模式。
- 占位块密度。
- 当前会话的独立设置。
- 已标记 turn 的本地标识。
- 用户手动输入的本地备注。
- UI 主题偏好。

这些设置保存在浏览器的 `chrome.storage.sync` 中。如果当前浏览器环境不支持 sync storage，则回退到 `chrome.storage.local`。

扩展不会把聊天正文写入 storage。已标记列表保存的是 turn 标识和基础状态；备注内容来自用户手动输入。

## 权限说明

扩展声明的权限为：

```json
{
  "permissions": ["storage"],
  "host_permissions": [
    "https://chatgpt.com/*",
    "https://chat.openai.com/*"
  ]
}
```

`storage` 用于保存扩展设置。

两个 ChatGPT 域名权限用于在 ChatGPT 页面注入 content script，实现本地 DOM 优化和中文悬浮设置面板。

## 网络行为

扩展自身不发起网络请求。

扩展不会向任何第三方服务发送数据，也不会接入任何远程脚本、统计服务或广告服务。

## 本地处理边界

扩展会读取页面 DOM 的结构信息，例如 turn 数量、turn 角色、节点高度和当前页面可见文本，用于生成目录、执行复制、生成导出预览以及决定是否收起旧消息或长用户消息。

这些信息只用于当前页面的显示优化，不会离开浏览器环境。
